# Telegram-бот тайм-трекера с геймификацией

## Что умеет
- Онбординг: ставка за час + цель по заработку.
- Главный экран с прогрессом, суммой, часами и кнопками.
- Баланс очков и скорость начисления на главном экране.
- Круговая диаграмма прогресса (заработано/осталось).
- Добавление времени вручную (`1:30`, `02:10:00`, `90`, `2ч 15м`).
- Подтверждение критических действий:
  - перед добавлением времени
  - перед сбросом прогресса
- Автообработка пересланного сообщения вида:
  - `🛑 таймер остановлен`
  - `Затрачено 03:59:32`
- История добавлений.
- Раздел `📊 Отчёты`: история + аналитика.
- Настройки: смена ставки, цели, режим уведомлений, сброс прогресса.
- Сброс прогресса с выбором: оставить цель или удалить цель.

## Геймификация
- `🛒 Маркет` в главном меню.
- Упрощённый UX маркета: `Покупки / Геймификация / Управление`.
- Начисление очков за подтверждённую работу (по формуле очки/минуту).
- Экономика очков настраивается в Telegram:
  - очков за минуту;
  - ручной бонус/штраф (`+30`, `-15`).
- Внутренний маркет:
  - быстрое добавление позиции одной строкой: `Название; цена; описание`;
  - опциональное фото товара (можно пропустить кнопкой);
  - покупка позиции со списанием очков;
  - ускоренная покупка: купить и сразу вернуться в список;
  - управление позициями (вкл/выкл, изменение цены, удаление);
  - история покупок и история транзакций очков.
- 🎰 Мини-казино beta:
  - спин за `10 ⭐`;
  - доступно только после входа в раздел `Казино`;
  - запуск кнопкой `Крутить`, сообщением/стикером/слот-эмодзи `🎰`;
  - выплаты за пару/тройку/джекпот (777);
  - корректное отображение барабанов (`BAR / 🍇 / 🍋 / 7️⃣`);
  - на джекпот — дополнительный бонус Freeze.
  - опционально можно включить новые кнопки Telegram:
    - `TG_BUTTON_STYLE=1` (стиль primary для кнопки спина);
    - `TG_CASINO_SPIN_ICON_EMOJI_ID=<id>` (premium custom emoji-иконка кнопки).
- Бонус-цели (можно несколько одновременно):
  - цели по деньгам или часам;
  - дедлайн: сегодня, до пятницы, до конца месяца или вручную;
  - автопроверка выполнения и автоначисление награды в очках;
  - архив выполненных/просроченных целей.
- Дисциплина в стиле Duolingo (метод «кнута»):
  - 🔥 стрик по рабочим дням (не по календарным);
  - 📅 настройка рабочих дней недели + override на сегодня/завтра;
  - 🧊 Streak Freeze (до 2 шт.), покупается за очки;
  - ⚔️ Streak Challenge на 7/14/30 рабочих дней со ставкой очков (пропуск рабочего дня = провал и потеря ставки);
  - 🏟 weekly-лига с повышением/понижением по минутам за неделю (есть бонус за ап и штраф за просадку).

## Запуск
1. Установи зависимости:
```bash
pip install -r requirements.txt
```

2. Создай `.env` на основе примера и вставь токен:
```bash
cp .env.example .env
```

3. Запусти бота:
```bash
python3 bot.py
```

## Хранение данных
- SQLite файл: `bot_data.sqlite3`
- Основные таблицы: `users`, `work_sessions`, `market_items`, `point_transactions`, `market_purchases`, `bonus_goals`.
- Дополнительные таблицы дисциплины: `habit_state`, `streak_challenges`, `discipline_day_overrides`.

## Дальше можно докрутить
- Top-дни/рекорды и streak (серия дней подряд).
- Еженедельные цели + прогресс по неделе.
- Экспорт истории в CSV/Google Sheets.
- Красивые шаблоны уведомлений с персональными советами.
